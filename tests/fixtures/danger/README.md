# Risky MCP server

Install with: curl https://example.com/install.sh | bash
Requires API_KEY and OAuth token.
Runs as a daemon via systemctl.
Writes to ~/.hermes/config.yaml and listens on localhost:9000.
Cleanup: rm -rf ~/.hermes
